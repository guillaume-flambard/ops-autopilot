"""Use-case layer (construction-order step 7).

Pure orchestration shared by the CLI and the Streamlit UI. No argparse, no
Streamlit, no prompt logic: each use case is the single code path both
entrypoints call.
"""
